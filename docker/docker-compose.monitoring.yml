# docker-compose.monitoring.yml
# Monitoring stack for baram with Prometheus and Grafana
#
# Usage:
#   docker-compose -f docker-compose.yml -f docker-compose.monitoring.yml up -d
#
# This adds:
#   - Prometheus (metrics collection and storage)
#   - Grafana (visualization and dashboards)
#   - PostgreSQL Exporter (PostgreSQL metrics)
#   - Redis Exporter (Redis metrics)

version: '3.8'

services:
  # ============================================================================
  # Prometheus - Metrics collection and time-series database
  # ============================================================================
  prometheus:
    image: prom/prometheus:latest
    container_name: baram-prometheus
    restart: unless-stopped
    # Security: Run as nobody user (UID 65534)
    user: "65534:65534"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:mode=1777
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=30d"
      - "--web.console.libraries=/usr/share/prometheus/console_libraries"
      - "--web.console.templates=/usr/share/prometheus/consoles"
      - "--web.enable-lifecycle"
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./monitoring/rules:/etc/prometheus/rules:ro
      - prometheus_data:/prometheus
    networks:
      - baram-network
    labels:
      - "com.baram.role=monitoring"
      - "com.baram.component=prometheus"

  # ============================================================================
  # Grafana - Visualization and dashboard platform
  # ============================================================================
  grafana:
    image: grafana/grafana:latest
    container_name: baram-grafana
    restart: unless-stopped
    # Security: Run as grafana user (UID 472)
    user: "472:472"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin}
      - GF_INSTALL_PLUGINS=grafana-piechart-panel
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://grafana:3000
      - GF_PATHS_PROVISIONING=/etc/grafana/provisioning
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana_data:/var/lib/grafana
    networks:
      - baram-network
    depends_on:
      - prometheus
    labels:
      - "com.baram.role=monitoring"
      - "com.baram.component=grafana"

  # ============================================================================
  # PostgreSQL Exporter - Metrics exporter for PostgreSQL
  # ============================================================================
  postgres_exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: baram-postgres-exporter
    restart: unless-stopped
    # Security: Run as nobody user (UID 65534)
    user: "65534:65534"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    environment:
      DATA_SOURCE_NAME: "postgresql://${POSTGRES_USER:-baram}:${POSTGRES_PASSWORD:?Please set POSTGRES_PASSWORD in .env}@postgres:5432/${POSTGRES_DB:-baram}?sslmode=disable"
    ports:
      - "9187:9187"
    networks:
      - baram-network
    depends_on:
      postgres:
        condition: service_healthy
    labels:
      - "com.baram.role=monitoring"
      - "com.baram.component=postgres-exporter"

  # ============================================================================
  # Redis Exporter - Metrics exporter for Redis
  # ============================================================================
  redis_exporter:
    image: oliver006/redis_exporter:latest
    container_name: baram-redis-exporter
    restart: unless-stopped
    # Security: Run as nobody user (UID 65534)
    user: "65534:65534"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    command:
      - "-redis.addr=redis:6379"
    ports:
      - "9121:9121"
    networks:
      - baram-network
    depends_on:
      redis:
        condition: service_healthy
    labels:
      - "com.baram.role=monitoring"
      - "com.baram.component=redis-exporter"

# ============================================================================
# Monitoring Volumes
# ============================================================================
volumes:
  prometheus_data:
    driver: local
  grafana_data:
    driver: local
